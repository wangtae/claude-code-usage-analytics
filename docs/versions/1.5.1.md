# Version 1.5.1

**Release Date:** 2025-10-27

## 🐛 Bug Fixes

### 권장 사용율 계산 로직 수정

Usage Limits의 주간 권장 사용율(recommended usage percentage) 계산 로직을 수정했습니다.

**변경 내용:**
- 7일을 1분 단위(10,080분)로 계산하도록 변경
- 권장 사용율 = (경과 시간 + 24시간) / 7일 × 100%
- 리셋 시점(0시간 경과): 14.3% 권장 (24시간 / 7일)
- 6일 경과 시점: 100% 권장 (168시간 / 7일)

**영향받는 함수:**
- `_calculate_weekly_recommended_pct()`: 주간 전체 모델 사용량 권장 퍼센티지
- `_calculate_distributed_recommended_pct()`: Opus 모델 주간 사용량 권장 퍼센티지

**기술적 세부사항:**
```python
# 변경 전
recommended_pct = (elapsed_minutes / total_period_minutes) * 100

# 변경 후
buffered_minutes = elapsed_minutes + (24 * 60)  # 24시간 버퍼 추가
recommended_pct = (buffered_minutes / total_period_minutes) * 100
```

이 변경으로 사용자는 주간 사용량을 더 효과적으로 분배할 수 있는 가이드를 제공받게 됩니다.

### 디버그 로그 제거

개발 중 추가된 디버그 로깅 코드를 제거하여 콘솔 출력을 정리했습니다.

**제거된 항목:**
- `_calculate_weekly_recommended_pct()` 함수의 stderr 디버그 출력
- `_calculate_distributed_recommended_pct()` 함수의 stderr 디버그 출력
- Exception 로깅 상세 정보

## 📝 Changed Files

- `src/visualization/dashboard.py`: 권장 사용율 계산 로직 수정 및 디버그 로그 제거

## 🔄 Migration Notes

이 버전은 하위 호환성을 유지합니다. 별도의 마이그레이션 작업이 필요하지 않습니다.